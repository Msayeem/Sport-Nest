"use client"
import React from 'react';


const DelUp = ({data}) => {
    return (
        <div>
             <button>Edit</button>
             <button>Delete</button>
        </div>
    );
};

export default DelUp;